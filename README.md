# 🚀 Система Сеплица - Полная инструкция по запуску

## 📋 Что включено

✅ **Лендинг** - высококонверсионная посадочная страница  
✅ **Backend API** - Node.js + Express сервер  
✅ **Админ-панель** - управление контентом, отзывами, заявками  
✅ **База данных** - JSON-based хранилище  
✅ **Загрузка файлов** - управление фотографиями

---

## 🎯 Быстрый старт (3 шага)

### Шаг 1: Установка зависимостей

```bash
cd backend
npm install
```

### Шаг 2: Первоначальная настройка

```bash
npm run setup
```

Программа попросит вас создать администратора:
- Введите username (или оставьте `admin`)
- Введите пароль (минимум 6 символов)

**Запишите эти данные - они понадобятся для входа!**

### Шаг 3: Запуск сервера

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

---

## 📂 Структура проекта

```
longevity-landing/
│
├── 📄 index.html              ← Главная страница лендинга
├── 📄 styles.css              ← Стили лендинга
├── 📄 script.js               ← JavaScript лендинга
├── 📄 README.md               ← Эта инструкция
│
├── 📁 backend/                ← Backend сервер
│   ├── server.js              ← Express API
│   ├── setup.js               ← Скрипт первой настройки
│   ├── package.json           ← Зависимости Node.js
│   ├── database.json          ← База данных (создается автоматически)
│   ├── README.md              ← Документация по backend
│   └── uploads/               ← Загруженные изображения
│       ├── hero/
│       ├── expert/
│       └── reviews/
│
└── 📁 admin/                  ← Админ-панель
    ├── index.html             ← UI админки
    ├── admin-script.js        ← Логика админки
    └── admin-styles.css       ← Стили админки
```

---

## 🌐 Доступ к сайтам

После запуска сервера откройте в браузере:

| Что | URL | Описание |
|-----|-----|----------|
| **Лендинг** | `http://localhost:3000/` | Основная посадочная страница |
| **Админка** | `http://localhost:3000/admin/` | Панель управления |
| **API** | `http://localhost:3000/api/` | REST API endpoints |

---

## 🔐 Вход в админ-панель

1. Откройте `http://localhost:3000/admin/`
2. Введите username и пароль из шага 2
3. Нажмите "Войти"

### Возможности админ-панели:

#### 📊 Статистика
- Общее количество заявок
- Новые заявки (последние 24 часа)
- Количество опубликованных отзывов

#### 🎨 Главный блок (Hero)
- Изменение заголовка
- Изменение подзаголовка
- Загрузка фото фона
- Текст кнопки

#### ℹ️ О системе
- Редактирование описания 4 шагов системы Сеплица
- Изменение заголовков и текстов

#### 👨‍⚕️ Об эксперте
- Имя эксперта
- Должность
- Цитата
- Биография
- Фото эксперта

#### ⭐ Отзывы
- Добавление новых отзывов
- Редактирование существующих
- Публикация/снятие с публикации
- Удаление отзывов
- Загрузка фото клиентов

#### 📧 Заявки
- Просмотр всех заявок с формы
- Изменение статуса (новая/в обработке/завершена)
- Удаление заявок
- Просмотр согласия на обработку данных

#### ⚙️ Настройки
- Телефон для связи
- Email
- Часы работы
- Адрес

---

## 💻 Команды для разработки

```bash
# Первоначальная настройка (только один раз)
npm run setup

# Запуск в режиме разработки (с auto-reload)
npm run dev

# Запуск в продакшене
npm start

# Установка зависимостей
npm install
```

---

## 🔄 Сброс пароля администратора

Если вы забыли пароль:

### Способ 1: Запустить setup заново
```bash
npm run setup
```
Выберите "yes" когда спросит о перезаписи базы данных.  
⚠️ **Внимание:** Это удалит все данные!

### Способ 2: Изменить хэш пароля вручную

1. Сгенерируйте новый хэш:
```bash
node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('новый-пароль', 10).then(hash => console.log(hash));"
```

2. Скопируйте полученный хэш

3. Откройте `backend/database.json`

4. Найдите секцию `users` и замените значение поля `password` на новый хэш

---

## 📤 Загрузка изображений

### Форматы:
- ✅ JPEG (.jpg, .jpeg)
- ✅ PNG (.png)
- ✅ GIF (.gif)
- ✅ WebP (.webp)

### Ограничения:
- Максимальный размер: **5 MB**
- Изображения автоматически сохраняются в `backend/uploads/`

### Куда загружаются фото:
- Hero фото → `uploads/hero/`
- Фото эксперта → `uploads/expert/`
- Фото в отзывах → `uploads/reviews/`

---

## 🚀 Деплой в продакшен

### Вариант 1: GitHub Pages (только лендинг)

1. Создайте репозиторий на GitHub
2. Загрузите файлы: `index.html`, `styles.css`, `script.js`
3. В Settings → Pages выберите main branch
4. Готово! Сайт будет доступен по адресу `https://username.github.io/repo-name`

⚠️ **Для backend потребуется отдельный хостинг!**

### Вариант 2: VPS/Cloud сервер (полный стек)

1. **Подготовка на сервере:**
```bash
# Установите Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установите PM2
sudo npm install -g pm2

# Клонируйте проект
git clone your-repo-url
cd longevity-landing/backend
npm install
npm run setup
```

2. **Запуск с PM2:**
```bash
pm2 start server.js --name seplitza
pm2 save
pm2 startup
```

3. **Настройка Nginx:**
```nginx
server {
    listen 80;
    server_name ваш-домен.ru;
    
    # Статические файлы (лендинг и админка)
    location / {
        root /path/to/longevity-landing;
        try_files $uri $uri/ /index.html;
    }
    
    # API
    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### Вариант 3: Heroku

1. Создайте `Procfile` в папке `backend`:
```
web: node server.js
```

2. Создайте приложение:
```bash
heroku create your-app-name
git push heroku main
```

3. Установите переменные окружения:
```bash
heroku config:set JWT_SECRET=your-super-secret-key
heroku config:set NODE_ENV=production
```

---

## 🔒 Безопасность

### ⚠️ ВАЖНО для продакшена:

1. **Измените JWT_SECRET**
   - Не используйте дефолтное значение
   - Создайте файл `.env` в папке `backend`:
   ```env
   JWT_SECRET=your-unique-super-secret-key-here
   PORT=3000
   NODE_ENV=production
   ```

2. **Настройте CORS**
   - В `server.js` замените `cors()` на:
   ```javascript
   app.use(cors({
       origin: 'https://ваш-домен.ru'
   }));
   ```

3. **Используйте HTTPS**
   - Установите SSL сертификат (Let's Encrypt бесплатный)
   - Настройте редирект с HTTP на HTTPS

4. **Ограничьте доступ к админке**
   - По IP адресу в Nginx
   - Или добавьте базовую HTTP аутентификацию

---

## 🛠 Troubleshooting

### Проблема: "Cannot find module 'express'"
**Решение:**
```bash
cd backend
npm install
```

### Проблема: "Port 3000 is already in use"
**Решение:**
```bash
# Найдите процесс на порту 3000
lsof -i :3000

# Убейте процесс (замените PID на номер из предыдущей команды)
kill -9 PID

# Или используйте другой порт в .env
PORT=3001
```

### Проблема: Форма не отправляется
**Решение:**
1. Проверьте, что backend запущен
2. Откройте консоль браузера (F12)
3. Проверьте ошибки в Network tab
4. Убедитесь, что URL API правильный в `script.js`

### Проблема: Не загружаются изображения
**Решение:**
1. Проверьте размер файла (макс 5MB)
2. Проверьте формат (только jpg, png, gif, webp)
3. Проверьте права доступа к папке `uploads/`

---

## 📞 API Documentation

Полная документация API в файле `backend/README.md`

### Основные endpoints:

```
POST   /api/auth/login          - Авторизация
GET    /api/content             - Получить весь контент
PUT    /api/content/hero        - Обновить hero секцию
GET    /api/reviews             - Список отзывов
POST   /api/forms               - Создать заявку
POST   /api/upload              - Загрузить изображение
GET    /api/stats               - Статистика
```

---

## 📝 Лицензия

Проект создан для Системы Сеплица.  
Все права защищены © 2024

---

## 🎉 Готово!

Теперь у вас есть полностью функциональная система:
- ✅ Красивый лендинг
- ✅ Работающая форма обратной связи
- ✅ Админ-панель для управления контентом
- ✅ Backend API для обработки данных

**Следующие шаги:**
1. Запустите `npm run setup` и создайте администратора
2. Запустите `npm run dev`
3. Откройте `http://localhost:3000/admin/` и войдите
4. Загрузите фотографии
5. Отредактируйте тексты под себя
6. Готово к работе! 🚀

**Нужна помощь?**
Проверьте файл `backend/README.md` для детальной документации.
